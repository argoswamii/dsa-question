#include<bits/stdc++.h>
using namespace std;

int main(){
	int n,m;
	cin>>n>>m;
	int a[n];
	for(int i=0; i<n; i++){
		cin>>a[i];
	}
	int b[m];
	for(int i=0; i<m; i++){
		cin>>b[i];
	}

	// sort(a, a+n);
	// sort(b, b+m, greater<int>());

	// int paper=0;;
	// int day=accumulate(b, b+m,paper);
	// paper=day+m;

	// cout<<paper;
	// int zero=0;

	// for(int i=0; i<n; i++){
	// 	if(a[i]==0) zero++;
	// }




}